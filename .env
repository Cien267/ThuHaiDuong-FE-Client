# Base URL API backend .NET.
# - Để trống: dùng "/api" → proxy Vite tới http://localhost:5129 (chạy repo local).
# - Khi xem preview trên Lovable: đặt http://localhost:5129/api để trình duyệt gọi thẳng
#   backend trên máy bạn (backend cần bật CORS cho origin của preview).
VITE_API_BASE_URL=
